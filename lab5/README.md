Начало лабы 5.

Смысл лабы: реализовать передатчик и приёмник UART.

Я начала с передатчика. У него есть триггер, по которому отправляются 8-битные данные. Машина состояний переходит между idle, start_bit, data_sending, stop_bit.
Переходы корректные, если смотреть на периоде 2 с.

Скоро добавлю выход из модуля передатчика: флаг TC -- transmission complete. И его буду сбрасывать.